{
    "version": 2,
    "functions": {
        "api/server": {
            "runtime": "nodejs14.x",
            "maxDuration": 30
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/server.js"
        }
    ]
}